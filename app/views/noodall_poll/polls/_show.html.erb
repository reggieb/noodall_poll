<div class="poll_response">
  <p><%= @poll.question %></p>
  <% for result in @poll.results %>
    <% for field in %w{text count} %>
      <%= content_tag('span', result[field.to_sym], :class => field) %>
    <% end %>
    <br />
  <% end %>
</div>
